export type Folder = {
    name: string
    artists: Artist[]
}

export type Artist = {
    id: string
    name: string
    portfolio: Image[]
}

export type Image = {
    id: number
    width: number
    height: number
    string: string
    thumbnail: string
}